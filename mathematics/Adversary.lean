import ObservationAlphabet
import Transport.FiniteMutualInfo

/-!
# Adversary model — active Eve, channel mutual information

`mutualInfo` is **derived** in `Transport/FiniteMutualInfo.lean` from
`Asymmetric.PosteriorUniform` — zero bits as corollary of uniform posterior,
not `mutualInfo := 0`. Extended metadata lives in **O⁺** (`MetadataSymmetry`,
`ParticipationSymmetry`).
-/

namespace ITS

-- Re-exported from `Transport.FiniteMutualInfo` (finite Shannon ITS).

end ITS
